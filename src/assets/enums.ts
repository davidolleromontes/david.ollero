export enum mainTabNavigation {
    employees = 'Employees',
    favorites = 'Favorites',
    me = "Me",
    messages = "Messages"
  }

 export enum MessageShape {
   text = 'text',
   image = 'image',
 }