export enum mainTabNavigation {
  employees = 'Employees',
  favorites = 'Favorites',
  me = "Me",
  messages = "Messages",
  repository = "Repository"
}

export enum MessageShape {
  text = 'text',
  image = 'image',
}